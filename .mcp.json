{
  "mcpServers": {
    "kb-writer": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp-server/dist/index.js"
      ],
      "env": {
        "KB_WRITER_API_BASE_URL": "${KB_WRITER_API_BASE_URL}",
        "KB_WRITER_ACCESS_TOKEN": "${KB_WRITER_ACCESS_TOKEN}"
      }
    }
  }
}
