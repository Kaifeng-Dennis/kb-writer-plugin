{
  "mcpServers": {
    "kb-writer": {
      "command": "node",
      "args": [
        "--experimental-strip-types",
        "${pluginDir}/mcp-server/index.ts"
      ],
      "env": {
        "KB_WRITER_API_BASE_URL": "${KB_WRITER_API_BASE_URL}",
        "KB_WRITER_BEARER_TOKEN": "${KB_WRITER_BEARER_TOKEN}"
      }
    }
  }
}
