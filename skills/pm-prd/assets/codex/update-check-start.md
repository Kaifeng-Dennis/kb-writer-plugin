Run `/bin/bash "<skill_root>/scripts/pm-toolkit-version" check` as a non-blocking startup preflight, resolving `<skill_root>` from this skill's absolute mounted path on the host. This script requires bash; never invoke it with `/bin/sh`. This check is not a workflow stage and must not pause for PM decisions unless an update package is already downloaded and verified. If it prints `UPDATE_READY`, tell the PM the current version, latest version, and that a verified package is cached, then ask whether to install now. Only if the PM explicitly agrees, run `/bin/bash "<skill_root>/scripts/pm-toolkit-version" install` and remind them to restart Codex or start a new conversation. If the PM declines, leave installed skills unchanged and continue. Never auto-install without confirmation. If the check fails or prints nothing, continue silently.
