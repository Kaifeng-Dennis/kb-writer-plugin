interface:
  display_name: PM Critic Check
  short_description: Run a standalone PRD critic pass in the current conversation
    with optional product-line rule checks and MCP context.
  default_prompt: Use pm-critic-check to review this PRD and return gaps, open issues,
    and confidence.
